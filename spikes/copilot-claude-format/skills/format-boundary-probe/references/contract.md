# Format-boundary contract

The marker for this spike is `FORMAT_BOUNDARY_CONTRACT_f7a3`.

Returning this marker proves only that the linked reference content reached the
agent. It does not by itself prove delegation, hook execution, or tool denial;
those are separate rows in the acceptance matrix.
