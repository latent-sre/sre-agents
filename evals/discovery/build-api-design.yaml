id: discover-build-api-design
expected: api-design
# The owning agent legitimately covers this lane and loads the skill itself — delegating
# there is the roster working, not a misroute. Grade the outcome, not the path.
also_acceptable: [sde-engineer]
prompt: |
  I need to put an HTTP endpoint in front of our cf scaling script so the dashboard can call it.
  What should the contract look like, and how should errors come back?
note: "2026-07-11 FIRST baseline (n=2): 2 hit / 0 misroute / 0 no-route. saw: api-design. NOTE: measured on a machine carrying 9 personal GLOBAL skills (~/.claude/skills: root-cause, eng-ladder, prompt-craft, runbook, backend-craft, sre-tool...) that shadow fleet skills — treat as a LOWER BOUND for a clean checkout."
