id: discover-method-self-improve-loop
expected: self-improve-loop
prompt: |
  I have a generator that drafts incident summaries and the quality is inconsistent. I want
  it to score its own output against a rubric and keep revising until it clears the bar —
  automatically and unattended. How should I structure that loop?
# Retargeted 2026-06 (PR #23 follow-up): the prior prompt ("a regression keeps coming back")
# was genuinely ambiguous with tdd-workflow (add the regression test) and pulled it 0/3 — a
# probe artifact, not a discoverability gap. This prompt is squarely self-improve-loop's lane
# (evaluator-optimizer + unattended "Ralph" loop), which tdd-workflow does not cover.
note: "namespace: CONTAMINATED (pre-clean-room; personal ~/.claude skills+agents and installed plugins were visible and competing — NOT comparable to a clean number). Retargeted to evaluator-optimizer / unattended-loop lane: 3/3 (old prompt 0/3 was a tdd-workflow probe artifact, not a skill gap)."
