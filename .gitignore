# Copilot-native artifacts generated by scripts/sync-copilot.* from the canonical .claude/ definitions.
# Both Claude Code and VS Code/Copilot read .claude/ directly, so these are optional. Regenerate any
# time with the sync script; don't hand-edit. Remove these lines if you prefer to commit them.
/.github/agents/
/.github/skills/

# Temporary git worktrees used for cross-branch review/comparison (scratch; never committed).
/.worktrees/
